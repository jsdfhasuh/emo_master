# Qt 自定义运行页面与多页面低代码机制 v1 实施计划

- 计划日期：2026-09-25
- 状态：`PLANNED / READY_FOR_PHASE_0`，尚未实施功能，尚未完成性能或硬件验收。
- 仓库：`jsdfhasuh/emo_master`
- 实施分支：`agent/runtime-workflow-architecture-v1`，继续在当前工作分支增量开发，不新建功能分支，不修改或合并到 `main`。
- 代码审查基线：`3d33dcafb525a087331e0d6f5c5034cde16e14e0`。
- 本次提交范围：仅新增本计划。后续功能按阶段独立提交，不与无关图标、算法或设备改动混合。
- 技术决策：沿用当前 Python 3.10 / PySide2 / Qt Widgets；不引入 Web 页面、QWebEngine、Node.js、QML 或 Qt 大版本迁移作为前置条件。
- 版本口径：计划提出 `project.json 2.2` 与 `presentation 1.0`；它们不是当前已支持格式。应用版本和包格式版本分别管理，不凭计划创建发布 tag。

## 1. 目标与范围

### 1.1 用户最终操作

在 emo_master Designer 中：新建页面 → 拖入图像、数值、文本等组件 → 从“流程数据”树拖入某个节点的输出完成绑定 → 新建其他页面并配置导航 → 用模拟或真实结果检查 → 保存草稿 → 发布项目包。

现场人员：导入已发布项目 → 检查设备与依赖 → 明确启动或连接已有运行任务 → 只通过运行页面观看结果、切页和查看指定结果详情。

低代码对象是“页面布局、组件属性、数据绑定和页面导航”，不是自动生成每个项目的 Python 窗口代码。修改这些配置不要求重新打包整个应用；新增组件实现或算法插件仍可能需要更新程序或依赖。

### 1.2 v1 必须包含

1. Designer 内独立的“流程设计 / 页面设计”工作区，保留原流程编辑和算子编辑器。
2. 多页面新建、重命名、排序、复制、删除、默认首页；顶部导航和页面跳转按钮。
3. 网格/分栏/容器布局，拖放、调整占位、属性编辑、复制删除、撤销重做。
4. 数据树自动列出当前项目的节点实例及其正式输出，支持属性选择与变量拖放两种绑定方式。
5. 图像、文本、数值、判定指示、运行状态、结果集合表格、导航按钮和布局容器。
6. 同一个输出绑定多个组件、多张页面，不复制算子、不重复执行或采图。
7. 同一套 Qt 渲染器用于编辑画布、模拟预览、实时预览、浮动预览和现场运行端。
8. 明确的结果作用域、不可变快照、图像资源身份、断线/过期状态、跨页共享订阅。
9. 独立现场入口、草稿与发布隔离、项目包导入校验、失败保留旧版本。
10. 源码、真实 gRPC、Windows 冻结包和不涉及真实设备的验收用例。

### 1.3 不纳入 v1 的内容

- 浏览器远程设计/看板、手机适配、云端部署、任意像素自由画布。
- 用户脚本、`eval` 表达式、任意 Python 导入、任意 QSS 或任意 `.ui` 执行。
- 在自定义页面中任意写 PLC、启动机器人、切换配方或修改算法参数。v1 自定义动作仅导航、冻结显示、恢复实时。
- 通过页面事件重新实现合格判定、PLC 握手或正式生产计数。
- 自动报警抢占翻页、定时轮播、自动下发并运行不受信任的项目包。
- 完整历史生产数据库、报表系统、跨工位产品追溯及其全量历史分页。v1 表格分页仅针对当前结果集合；有限快照回看不等于长期历史系统。
- 强制改造所有算子或重写整个 MainWindow。没有配置页面的项目仍按原流程使用。

## 2. 已核查的代码与改造依据

以下是上述提交的代码事实，不以早期架构计划中描述的旧状态作为当前事实。路径均相对仓库根目录。

| 位置 | 当前事实 | 本计划要求 |
| --- | --- | --- |
| `src/emo_master/core/project/models.py` | `ProjectDocument` 接受 2.0/2.1，严格禁止额外字段，没有页面模型 | 正式增加版本化页面契约，不能临时塞字段 |
| `src/emo_master/core/project/migration.py` | 规范格式为 2.1，进行旧格式迁移与严格校验 | 扩展到 2.2，验证旧格式后迁移，保持幂等和不修改输入 |
| `src/emo_master/apps/designer/state/workflow_store.py` | 独立于 Qt 画布管理工作流，显式加载项目字段 | 新页面状态必须在完整保存/加载中保留，防止保存流程时丢页面 |
| `src/emo_master/apps/designer/ui/main_window.py` | 已有工作流标签、中央画布、左右区域、运行控制和日志 | 主窗口仅集成工作区，不继续堆页面模型和结果处理 |
| `src/emo_master/apps/designer/services/runtime_worker.py` | `_runWorker()` 先启动 Job，再 follow 事件 | 拆出只订阅已有运行的数据路径，不能打开页面就启动任务 |
| `src/emo_master/apps/designer/services/runtime_client.py` | 已有 DTO、gRPC/内嵌适配和取消；客户端与 Designer 目录及部分 Runtime 实现有依赖 | 渐进抽出可共享客户端，保留旧导入兼容入口，不能让运行端依赖 Designer 主窗口 |
| `src/emo_master/apps/runtime/workflow/runner.py` | 发布节点/工作流完成及 outputs；`_jsonSafe()` 对无法序列化对象可能退化为字符串 | 不从 ndarray 字符串恢复图像；结果发布要走类型化、独立的资源通道 |
| `src/emo_master/apps/runtime/preview/store.py` | `PreviewSnapshotWriter.capture()` 同步编码图像，按工作流/节点/端口覆盖暂存文件；类型有白名单 | 旧算子调参快照不能直接充当多次检测的实时结果总线 |
| `src/emo_master/apps/runtime/workflow/context.py` | 有 Job、工作流调用、节点运行、父调用及循环路径，没有专用业务检测快照身份 | 补足调用位置与结果作用域，不把 Job 等同于一件产品 |
| `src/emo_master/apps/runtime/events/event_store.py` | 事件带 Job 内序号；append 在锁内调用要求非阻塞的 sinks | 禁止在 sink 中编码图像、写展示文件或等待 UI/网络 |
| `src/emo_master/apps/runtime/jobs/worker_main.py` | spawn worker 发布 JSON-safe 事件，使用 `eventQueue.put()`；心跳也经该队列 | 展示压力不能挤占原有 Job 生命周期/心跳通道；不得把原通道改成随意丢事件 |
| `src/emo_master/apps/runtime/main.py` | gRPC 线程池 `max_workers=4` | 必须验证长订阅不耗尽控制请求执行资源 |
| `src/emo_master/apps/windows_entry.py` | 主要入口进入 Designer，仅另有自检分派 | 新增运行服务和现场界面入口，冻结模式要实测 |
| `src/emo_master/core/project/package_builder.py` | 构建 `.vxpkg`，递归收集目录；存在硬编码的版本信息 | 改为校验后的资源清单，避免打入运行数据、凭据或输出包自身 |
| `src/emo_master/core/project/repository.py` | 已有临时文件、fsync、替换和备份写入 | 复用原子保存思路，不另做不安全的页面单文件覆盖 |

端口依据见 [算子输入输出契约](../operator-io-contracts.md)。现有 `blob_analysis/manifest.json` 声明 `blobs/mask/overlay/frame`，其中 overlay 可选且 drawOverlay 默认关闭；`collection_count/manifest.json` 声明整数 `count`。这些作为绑定验收样例，不虚构现有算子的 `isOK` 输出。

## 3. 交互规格

### 3.1 页面设计工作区

左侧设“页面 / 组件 / 流程数据”标签；中央显示当前页面；右侧设“外观 / 数据 / 动作”属性页。顶部保留项目保存和明确的运行控制，页面编辑与开始运行不是同一操作。

页面管理：按稳定 pageId 管理。复制时重新生成 pageId、componentId，重映射副本内部引用；复制页面中的数据来源保持原绑定并显示确认提示，不能按新名称猜前后工位。删除被导航引用的页面应列出引用并允许取消或显式修改；删除后未修复的绑定不能发布。

布局：第一版使用有限嵌套的网格容器及行列占位/跨度，不保存绝对屏幕像素为唯一布局。支持拖动换位置、拉伸跨度、分栏比例、对齐、间距和最小尺寸。不同组件不允许无提示重叠；小窗口采用最小尺寸/滚动策略，不无限缩小文字。

编辑动作进入命令栈。一次拖动只记录一个命令；连续属性编辑可合并；页面删除及引用修复为一个事务。普通导航和实时数据刷新不进入撤销栈，不把项目标为已修改。

### 3.2 直接绑定节点输出

“流程数据”显示：工作流 → 节点实例〔显示名称、算子类型〕→ 输出端口 → 已知类型字段。未运行时也可绑定，当前值显示“尚未运行”。只注册但没有添加到项目的算子不出现在这个树中。

支持两种等价交互：

- 选中控件 → 数据属性 → 选择来源 → 选择工作流/节点/端口/字段。
- 将数据树中的端口或字段拖到控件上 → 选择兼容属性 → 确认绑定。

不强制手工注册全局变量，不强制把所有节点结果提升为工作流对外输出。对外输出可作为另一种稳定数据源，但不是直接绑定的前置条件。

不兼容来源禁用并说明原因。绑定成功显示来源路径、类型、状态；提供“定位来源节点”。新增相同类型的算子应自动可用。对可选 overlay 显示配置提示，不自动修改 drawOverlay 或启动采图。

### 3.3 多页面与详情

运行壳提供不可被页面隐藏的连接状态、数据年龄和异常提示。顶部导航来自 pageOrder；组件按钮通过 `navigate(pageId)` 切页，不在工作流增加翻页算子。

普通切页使用目标页面所需作用域的最新完整快照。点击“查看这次详情”则携带 resultKey，进入锁定模式；不偷偷转到更新的 OK。锁定内容过期时显示过期，并可“返回实时”。有限回看不能承诺任意历史均可用。

跨工位总览中，各工位显示各自的检测身份和时间；不声称不同工位天然属于同一产品。需要跨工位关联属于后续显式业务追溯。

### 3.4 预览与现场边界

模拟模式默认启用且固定标识，不访问设备、不写生产计数。实时模式只连接明确选择的已有 Job/会话；无任务显示未连接，不自动 StartJob。第一版在没有会话跟随机制时要求显式选择 Job，不能全局挑“最后一个任务”。

浮动预览与内嵌页面共享一个 DisplaySession。关闭预览或切页只取消对应观察者，不 StopJob。暂停画面只冻结快照，不暂停流程。固定的运行控制操作单独显示，不混入自定义导航动作。

同一运行任务使用启动时冻结的工程/绑定配置。草稿变更后，只可继续显示与当前发布计划兼容的来源；新增尚未采集的来源显示“需用于下一次运行”，不能为预览热改运行中流程。布局和文字改动可本地预览，现场发布版本不受影响。

## 4. 模块边界与依赖

以下是拟新增位置，实施时可在保持职责的前提下微调文件名，不可伪装成已有接口。

| 模块 | 建议位置 | 边界 |
| --- | --- | --- |
| 页面、组件、绑定、动作、结果 DTO 与校验 | `core/presentation/` | 纯 Python/Pydantic，不 import Qt、apps、protobuf 或算子实例 |
| 项目编辑协调器和页面状态 | `apps/designer/state/presentation_store.py`、`project_edit_session.py` | 工作流/页面共用一份项目草稿和保存事务，不各自覆盖 project.json |
| 页面工作区与绑定选择器 | `apps/designer/page_designer/` | 仅负责编辑，不实现检测逻辑 |
| 共享原生 Qt 组件/渲染器/导航 | `ui/presentation/` | 不 import Designer MainWindow、算子编辑器或 Runtime 执行器 |
| 展示客户端、DTO 适配、只读会话 | `clients/runtime/` | 可被 Designer 和运行端复用；Qt 线程适配留在 UI 层 |
| 结果采集、聚合、快照、资源库 | `apps/runtime/presentation/` | 不创建 QWidget，不等待页面，不直接做业务判定 |
| 独立现场界面 | `apps/operator_view/` | 读取发布版，只有共享渲染器和必要运行壳 |

组件注册描述包含：组件类型及版本、可编辑属性、可绑定属性及类型、渲染工厂、默认配置、允许动作。它与算法插件注册分离；不要求每个算法配一套 UI。v1 只加载随应用发布的受信任组件，项目包不能通过类路径加载代码。

从现有 RuntimeClient 渐进提取共享部分，旧模块保留兼容导入。多观察者的订阅所有权由 DisplaySession 管理，不能因关闭一张页面就调用全局取消。共享模块不把 RuntimeService、全局计数实现或 Designer 窗口作为导入依赖。

Qt 运行页面采用 QWidget 原生组件、网格/分栏布局和 QStackedWidget 页面宿主；编辑器用同一渲染器叠加选择/拖放层。Graphics View 限用于图像及几何交互等适合的区域，不把全部 QWidget 包装进代理图形项以追求编辑缩放。

## 5. 项目与页面数据契约

### 5.1 单一项目来源

规范项目升级为 2.2，增加 `presentation`，内部版本为 1.0。旧项目迁移为无页面状态，不自动添加设备、运行任务或假业务输出。

`presentation` 建议包含：

- `schemaVersion`、`defaultPageId`、`pageOrder`、`pages`、`navigation`。
- `resultScopes`：一次展示结果的工作流调用边界与调用位置。
- `dataSources`：由绑定操作自动生成/复用的来源定义，不要求用户手工建立变量表。
- 页面含稳定 pageId、名称、组件树、布局；组件含 componentId、type/version、props、bindings、actions。

下例是拟议结构片段，只展示数值绑定和两页导航；`main/count-node` 是示例节点，不是仓库已有项目 ID。

```json
{
  "schemaVersion": "2.2",
  "presentation": {
    "schemaVersion": "1.0",
    "defaultPageId": "overview",
    "pageOrder": ["overview", "detail"],
    "navigation": {"mode": "tabs"},
    "resultScopes": {
      "main-result": {
        "entryWorkflowId": "main",
        "scopeWorkflowId": "main",
        "callPath": [],
        "commitPolicy": "workflow_terminal"
      }
    },
    "dataSources": {
      "target-count": {
        "kind": "node_output",
        "resultScopeId": "main-result",
        "workflowId": "main",
        "callPath": [],
        "nodeId": "count-node",
        "port": "count",
        "fieldPath": [],
        "expectedType": "integer"
      }
    },
    "pages": {
      "overview": {
        "name": "运行总览",
        "layout": {"type": "grid", "columns": 2},
        "components": [
          {
            "componentId": "count-card",
            "type": "number",
            "version": "1.0",
            "layout": {"row": 0, "column": 0},
            "props": {"title": "目标数量", "unit": "个", "emptyText": "—"},
            "bindings": {"value": "target-count"}
          },
          {
            "componentId": "detail-button",
            "type": "navigation_button",
            "version": "1.0",
            "layout": {"row": 0, "column": 1},
            "props": {"text": "查看详情"},
            "actions": {
              "clicked": {"type": "navigate", "pageId": "detail", "context": "live"}
            }
          }
        ]
      },
      "detail": {
        "name": "检测详情",
        "layout": {"type": "grid", "columns": 1},
        "components": []
      }
    }
  }
}
```

### 5.2 校验与迁移

结构校验和发布校验分离。草稿允许“尚未绑定”的合法编辑状态，但发布要拒绝悬空 pageId/nodeId/port、类型错误、未决循环调用选择和缺失资源。未知字段、非法枚举、非法数值、重复 ID、循环组件树仍严格拒绝，不能以草稿为由接受任意 JSON。

按原版本严格校验后做 1.x → 2.1 → 2.2 的明确迁移；不能先用新模型填充未知字段再掩盖旧格式错误。原输入不变、迁移幂等、旧工作流/Loop 契约保留。旧程序不支持 2.2 时明确拒绝，不允许静默删页面再保存。保存提供备份，降级使用旧备份。

页面配置保存到项目；Dock位置、窗口位置、编辑缩放、活动页等个人偏好保存到本机设置。真实结果、图像缓存、实时 taskId 不进入项目源文件。

项目整体导出包含页面。现有工作流局部导入导出继续保持原语义，v1 不隐式夹带页面；以后支持带页面片段时必须实现 ID 映射和未解析绑定预览。

## 6. 数据绑定与类型规则

静态来源身份是 resultScope + 工作流调用路径 + workflowId + nodeId + outputPort + 字段路径；运行身份另外携带，不把 operatorId 或显示名当作唯一数据地址。

来源目录以项目节点实例及规范化端口契约为准；与实际加载的插件版本比较。节点存储的简写类型与插件 PortSpec 冲突时报告错误，不能只取较宽松的一方。算法局部变量、日志文本、私有对象不自动暴露。

v1 支持：

| 来源 | 可绑定组件 | 限制 |
| --- | --- | --- |
| integer / number | 数值、文本 | 保留整数/浮点区别，boolean 不自动当数值；单位和小数位仅格式化 |
| string / boolean | 文本、判定指示 | 显式值到标签/颜色映射；无值不当 false/NG |
| image | 图像 | 通过受控 ImageRef 读取，不传 Qt 对象，不猜文件路径 |
| 已知 geometry / collection | 叠加、结果表格、字段选择 | 使用已有契约与坐标空间；不自动取列表第一个对象 |
| runtime_status | 平台状态 | 与业务 OK/NG 分开，只读 |
| 已有命名 global_counter | 计数卡片 | 读取 Runtime 权威值，页面不自行累加或清零 |
| workflow_output | 与其类型一致的组件 | 可选稳定边界，不代替直接节点绑定 |

字段选择第一版覆盖已有、明确的语义类型，如 Blob 的 area/centroid 和 Detection 字段。通用 json/any 默认仅作有大小限制的文本/原始数据查看；没有结构定义不得承诺自动展开任意字段。动态列表的行由集合决定；单值选择需先在流程内筛选/选择，或采用明确且可校验的选择器，不默认第一项。

显示层只做格式化和表现映射，不新增“数量等于 4 即合格”的业务计算。流程执行 COMPLETED 不等于产品 OK。统计用已有正式输出/计数；没有合格率源时不虚构历史统计，新增业务计算应在流程或独立统计模块实现。

## 7. 一次检测、调用路径与快照一致性

### 7.1 结果作用域

简单无循环流程默认以指定根工作流的一次调用为结果边界。循环检测推荐以“一轮检测所在子工作流的一次调用”为边界。作用域由绑定向导自动建议；重复调用无法唯一确定时必须要求选择，不能悄悄取全局最后一条。

结果键建议为 `(runtimeInstanceId, projectId, executionRevision, jobId, resultScopeId, scopeWorkflowRunId)`。`scopeWorkflowRunId` 标识一次实际调用；可另有用户可读检测序号。序号不是跨重启/跨 Job 唯一主键。

补充稳定调用位置路径，含父工作流、调用节点、Subflow/Loop body/condition 角色。现有 parentWorkflowRunId 和 iterationPath 用于关联但不能单独代替调用位置。两处调用同一个子工作流必须区分。

绑定输出需能唯一解析到该结果作用域内的一次执行。循环内多次出现的内部节点不能默认“最后一次”；应绑定 Loop 聚合输出或选择更内层作用域。两组不同作用域的结果可同页展示，但必须分别标注，不能伪装成原子的一次产品检测。

### 7.2 快照内容与完成规则

Runtime 将所需输出聚合成不可变 ResultSnapshot，包含结果键、工程执行修订、快照版本/序号、来源值、各值状态、ImageRef、事件游标、发生时间和采集诊断。

每个来源至少区分 `VALID / MISSING / SKIPPED / FAILED / UNAVAILABLE`；连接状态与陈旧程度是另外维度。合法空集合、数值 0、布尔 false 不等于缺失。

同一作用域到达 workflow 终态后封闭该次结果，正常或失败都能生成对应状态。正常完成但某可选端口没有输出时显示缺失，不沿用上一轮。流程中途失败应发布该次失败/不完整状态，不能让上一轮 OK 冒充当前结果。

运行状态可实时更新；业务结果默认按终态快照整体提交。需要中间节点诊断时放在明确标记的诊断区域，不混入正式结果卡片。长期运行但未划定逐件工作流边界的流程，发布检查应提示配置作用域，不能无限等 Job 结束。

### 7.3 图像和跨页原子显示

图像标识必须关联结果键、端口、源帧/相机身份和不可变资源 ID；不能只使用固定 sourceId 字符串当逐帧身份。坐标空间使用已有 frame/transformToSource/homographyToSource 契约。

后台准备一组快照，图像就绪或明确判为不可用后，UI 主线程以一次模型提交更新关联图像、框、数值和判定。显示加载中/不可用可以，混入上一轮图像不可以。

图片读取结果需携带会话 generation + resultKey + view requestId；切页、重新绑定、换项目或重连后，迟到的旧请求不能覆盖新数据。缓存键也必须包含不可变资源身份。

冻结/详情锁定的是结果键及快照，不是几个控件当前文字。导航历史有上限；快照租约过期显示“已过期”，不自动替换为最新结果。

## 8. 结果与图像发布通道

### 8.1 与现有事件/预览分工

已有 Job/EventStore/SQLite 仍负责执行事件和原有重放，不因 UI 刷新策略丢弃正式事件或计数。旧算子预览 API 保持兼容。

新增结果采集计划由已发布绑定编译得出，只采集需要展示的数据。运行时不能按浏览器式“每个组件一个观察器”反复调用算法。相同来源在多个页面只采集一份。

新展示通道在数据序列化前取得经过校验的输出，不从日志、message 或 ndarray 的字符串表示解析业务值。无 presentation 的项目不增加新展示图像编码工作。

### 8.2 有界异步图像导出

worker 内只在预算允许时复制需要显示的图像，必须拥有独立缓冲，避免下游原地修改 ndarray 导致串图。检查尺寸、dtype、通道及字节预算在复制前进行；过大数据返回明确展示诊断。

编码/预览缩放由专用有界导出任务执行，不在 EventStore sink、gRPC 控制线程或 Qt 主线程执行。复制本身有成本，必须计入基准；不得宣称“绝对零开销”或仅靠加线程就消除了资源竞争。

跨进程只传 JSON-safe 结果/资源描述，图像经 Runtime 分配的受控暂存区写入，临时文件完成后原子发布。一次结果的文件名不可覆盖上一结果。禁止任意路径、目录穿越、符号链接越界和按客户端路径直接读文件。

展示描述走独立有界队列，不复用原心跳/生命周期队列的大图路径。慢消费者允许合并或跳过中间展示快照，但保留最新状态并报告跳过数量；不能无界积压。不得通过改原 eventQueue 为丢包来“优化”。

在 Job 完成、失败、强制停止时规定导出任务的取消和有界收尾；不能无限等图片。没有导出完成的图像明确标为不可用。展示暂存资源在原 job workspace 清理前完成接管或取消，不能依赖已删除目录；强制退出后的残留由 Runtime 下次启动按会话归属清理，不能误删另一个仍存活的 Runtime 数据。

### 8.3 存储与资源寿命

Runtime 维护有界的最新快照与有限回看缓存；图片按项目/会话计数与字节双限额管理。锁定回看可申请有期限租约，必须有总量上限，不能靠无限 pin 绕过磁盘上限。

图像过期与事件仍可重放是允许状态，需明确返回资源不可用。第一版不把有限展示缓存宣传成生产历史存储。现有正式输出文件不应因展示缓存清理被删除。

## 9. Runtime API 与客户端生命周期

在现有 proto 体系中增量增加类型化只读接口，具体名字以实施提交为准，能力必须覆盖：

| 拟议接口 | 作用 |
| --- | --- |
| GetRuntimeCapabilities / GetRuntimeIdentity | 协议版本、运行实例、支持的显示能力 |
| ListJobs | 按项目查询可连接任务，解决新运行端没有 Job ID 的问题 |
| ListDisplaySources | 返回已加载工程的来源目录与采集计划版本 |
| GetDisplaySnapshot | 返回指定作用域当前快照及一致的 high-water cursor |
| StreamDisplayUpdates | 从游标开始订阅；先补增量再实时跟随，可取消 |
| GetDisplayAsset | 按受控 assetId 有界分块读取及校验，不接受任意绝对路径 |
| Pin/ReleaseDisplaySnapshot | 有期限、有限额的详情查看租约，可选内部合并为句柄 API |

快照获取与订阅必须防止中间漏消息：同一锁/版本屏障生成快照和游标，随后获取严格大于游标的更新；历史已截断返回 RESET_REQUIRED 并重新取快照。新增展示序列按发布范围定义，不混用多个 Job 各自从 1 开始的 event.sequence。

发布实例变化、Runtime 重启或工程执行修订变化时更新 generation 并清理旧会话缓存。禁止全局 lastSequence 导致换 Job 后拒收新数据；收到重复消息幂等处理，不通过重放增加计数。

每个客户端/明确运行会话只有一个 DisplaySession，页面和浮动预览是其观察者。失去一个观察者不取消其他页面；最后观察者退出释放自身通道、读图任务和租约，不关闭不属于自己的 RuntimeService。

所有长期流（包括旧 Job follow 和相机预览）需统一核查资源占用。v1 在同步 gRPC 服务器对长期流执行非阻塞配额准入：以当前 4 工作线程为基线，最多 2 个长期流，至少留下 2 个名义控制处理槽；超限快速返回 RESOURCE_EXHAUSTED，不能在 worker 中等待信号量。每个出口在取消/异常/正常结束时 finally 释放配额；图像传输有独立并发及超时限额。

上述配额不是硬实时保证。P0/P2 必须用真实网络验证停止和状态请求；若共池方案无法通过门槛，进入独立展示执行池/只读服务的设计修订，不能只无限增大线程数或牺牲控制响应。控制和展示均只使用同一个 Runtime 所持有的项目/设备资源，不创建第二套 Runtime 实例。

默认只支持本机 loopback。已有远端使用不能倒退，但新增页面能力未经过认证、授权和网络验收前不作为局域网控制功能交付。没有能力的新服务端应明确报告不支持，不能悄悄退回“预览打开就启动任务”。

## 10. Qt 页面实现与线程规则

QStackedWidget 仅负责页面显示；PageNavigator 负责稳定 pageId、返回栈和 live/pinned 上下文。不要保存 QWidget 指针或 tab index 作为页面业务标识。

页面按需实例化，运行端不 import 页面编辑器和算子编辑器。缓存页面数、集合行数、图像字节数都有上限。隐藏页面停止不必要的解码和绘制，但不关闭 Runtime 采集，也不漏正式统计。

GUI 主线程只创建/修改 QWidget 与 GUI 图形对象。接收、资源读取、解码在后台；使用自有缓冲的 QImage/字节数据，不能让 QImage 引用已被释放或复用的 NumPy 内存；QPixmap 转换/设置留在主线程。按 generation 丢弃迟到任务，有界合并 UI 刷新，不能每个事件无限排一个 signal。

表格使用模型/视图与分页/按需行访问，不为每个单元创建 QWidget。v1 分页绑定同一快照的当前集合，翻页和排序不改变检测结果源；切到新快照重置或明确保留筛选，不混合新旧行。长期历史查询不在本次承诺内。

颜色之外同时显示文字/图标，确保 OK/NG/未检测/运行失败可区分。保留平台级断连、陈旧、模拟和冻结标记。导航页缺失、组件不支持等问题用占位错误呈现，禁止悄悄替换成看似正常的内容。

发布验收覆盖 100% / 125% / 150% 缩放、窗口最小尺寸、多屏移动、长中文名称、长错误文本及键盘导航。共享渲染器不等于不同设备必然像素一致。

## 11. 保存、发布、现场入口与安全

### 11.1 草稿与发布

ProjectEditSession 统一组装 workflow + presentation，原子保存和备份；编辑页/流程时不能相互覆盖。迁移与保存检查必须覆盖 WorkflowStore 的所有导出入口，而非只改 ProjectDocument。

发布将项目修订、依赖清单、页面配置和资源清单冻结为不可变 revision。执行修订/采集计划另有指纹，纯页面样式变化不伪装成算法改变。草稿绑定来源改变不能热修改正在运行的采集计划。

沿用 `.vxpkg`，升级 manifest 的兼容性字段和完整性校验；如果语义不向后兼容则提升 manifest 版本。构建采用明确 allowlist：项目、必要模型、图像/图标资源与依赖声明；默认排除运行 DB、日志、缓存、测试输出、令牌、临时文件和构建目标。包输出位于项目外或显式排除；拒绝越界链接。

包内依赖版本从实际检查结果产生，不能沿用硬编码 `0.2.0` 表示新能力。哈希只证明完整性，不等于可信签名；v1 只运行可信来源项目，禁止包中脚本/未知 Python 插件自动安装执行。

### 11.2 导入和版本启用

导入先在独立暂存目录验证：路径、大小、重复项、压缩炸弹限额、资源哈希、schema、组件版本、绑定和插件兼容性。验证全部通过后才原子启用；失败保留旧发布版。设备地址/相机序列号等站点配置单独映射，不能因为拖入项目包就自动触发。

项目启用与检测启动是两次操作。运行中默认拒绝切换执行版本，要求明确停止或现场认可的安全切换点。回退切回上一完整发布版；数据库/项目格式不兼容时使用备份，不能强行降级后静默丢字段。

### 11.3 启动形态

新增拟议入口：`EmoMaster.exe --runtime` 与 `EmoMaster.exe --operator-view ...`；无参数继续进入 Designer，`--self-test` 保留。参数名在 P5 固化并同步开发/部署文档，实施前不可当现成命令使用。

现场启动器可以启动一个独立 Runtime，再连接 Operator View；多个客户端不得重复争用相同数据目录或设备。服务就绪、项目就绪、页面就绪分别报告，禁止以固定 sleep 代替握手。端口冲突明确报错，不杀死未知进程。

关闭 Operator View 只断开显示；独立 Runtime 的停止由明确控制决定。Designer 内嵌模式的进程退出仍会影响其拥有的 Runtime，不能宣传与独立部署同等隔离。即便界面可独立退出，设备故障/丢失监控时是否停机仍由明确现场策略决定。

默认不自动开始设备流程。后续若配置自动运行，必须是已批准发布版本、明确启动策略和设备验收，不借“自动打开首页”隐式实现。Windows freeze_support、spawn、退出回收和无 GUI 的 Runtime 导入分别测试。

## 12. 分阶段任务与退出条件

所有阶段在当前工作分支增量提交。先完成数据正确性最小链路，再扩大编辑功能；不能先做一个看起来可拖动但只能读取假数据的“完整低代码平台”。

### P0：基线、契约和风险原型

- [ ] 对比分支最新 HEAD 与审查基线，列出差异；保留他人新增内容。
- [ ] 在可运行环境记录现有 CI 结果，失败区分基线和新增；检查测试命名后补针对性用例。
- [ ] 用本地图像/模拟输出验证根流程、循环体、重复子流程的结果作用域，以及调用路径补充方案。
- [ ] 验证有界异步图像导出、Job 退出收尾、旧快照不串件和原心跳通道隔离。
- [ ] 验证长期流配额与控制响应，记录实际图像大小、速率、延迟与内存。

退出条件：先把结果身份、资源寿命、调用歧义和线程资源方案写成可执行契约测试；失败则修订本计划相关决策，不能跳过转入大规模 UI。

### P1：项目模型、绑定目录与持久化

- [ ] 新增 core/presentation 模型、结构/发布校验及版本迁移。
- [ ] 统一 ProjectEditSession 与现有 WorkflowStore 保存/加载；页面草稿不丢失。
- [ ] 来源目录、规范化类型、已知集合字段、直接节点/工作流输出绑定。
- [ ] 多页面 CRUD、稳定 ID、导航引用、复制映射和命令模型。

退出条件：无 Qt 环境能验证项目 round-trip、迁移、无效绑定与多页面引用；未运行流程时可列出来源。

### P2：Runtime 结果通道与共享客户端

- [ ] 实现作用域采集计划、调用上下文、类型化结果封闭、资源导出/接管/清理。
- [ ] 新增快照+游标+订阅+资源接口、能力协商与明确 Job 选择。
- [ ] 拆出只读 DisplaySession，保留原运行控制和客户端兼容入口。
- [ ] 长订阅准入、限额、取消释放；错误、断线、重启、资源过期处理。

退出条件：真实 gRPC + spawn 测试中图像/数值不串件；新观察者不开新 Job；异常页面不阻塞停止；有限缓存不宣称完整历史。

### P3：共享 Qt 渲染器和最小运行页面

- [ ] 先实现图像、数值、文本、判定、状态、集合表格、导航按钮、容器。
- [ ] QStackedWidget 导航、公共状态栏、按需页面、live/pinned 上下文。
- [ ] 用手写测试配置验证同页及跨页的数据更新，模拟和真实模式使用相同渲染器。
- [ ] 真实 Qt 事件循环测试迟到请求、切页、隐藏页、表格分页和释放。

退出条件：两页“Blob 图像+计数”真实链路可用；反复切页不增加任务、设备连接或订阅；先证明数据正确再做拖拽。

### P4：Designer 低代码编辑工作区

- [ ] 页面列表、组件面板、来源树、属性/动作面板和容器拖放。
- [ ] 选择绑定、变量拖放绑定、类型限制、定位来源、可选输出提示。
- [ ] 网格/分栏调整、复制删除、撤销重做、默认页、导航按钮。
- [ ] 内嵌与浮动预览、模拟标记、选择已有 Job；运行中草稿兼容性提示。

退出条件：不写代码完成三页设计，保存重开仍保留全部绑定，复制工位页不误绑，发布前能定位每个错误。

### P5：发布包、独立运行与部署入口

- [ ] 资源 allowlist、版本/依赖/哈希、项目包安全导入、设备映射。
- [ ] 发布版本启用、失败保留旧版、回退；运行中版本切换保护。
- [ ] Runtime/Operator View Windows 参数分派、进程所有权、就绪握手、自检。
- [ ] 更新外部构建仓库所需资源清单的对接说明，不在本计划提交时跨仓改动。
- [ ] 同步 README、开发部署、项目格式、运行事件和新增页面使用文档。

退出条件：离线 Windows 交付包可导入并显示已发布多页项目，不加载编辑器；旧入口不退化；关闭页面不会误停独立 Runtime。

### P6：回归、性能和现场准入

- [ ] 跑完整检查、真实网络/进程、真实 Qt 和冻结包测试。
- [ ] 运行不少于 30 分钟的模拟持续检测与切页/重连压力验证。
- [ ] 记录不开页、一页、多页、多客户端的资源/延迟/控制请求对比。
- [ ] 完成缩放、长文本、缺失图像、错误提示和无网络依赖验收。
- [ ] 真实相机/PLC 另行审批及现场验证，保留旧程序/发布包回退路径。

退出条件：结果一致性零容忍用例全通过，性能在约定预算内；软件自检、截图和真实硬件验证分别记录，不能互相替代。

## 13. 必须通过的验收矩阵

| 编号 | 验证 | 通过标准 |
| --- | --- | --- |
| A01 | 旧 1.x/2.0/2.1 项目迁移、重复迁移 | 工作流语义保留，页面为空，输入对象不变，幂等 |
| A02 | 新页面保存、重开、编辑流程再保存 | 页面/绑定/导航不丢失，结构化未知字段不被吞掉 |
| A03 | 未运行时列出节点数据 | 有端口与类型，无伪造当前值；只注册未实例化算子不混入 |
| A04 | 两个同类计数节点、重命名 | 绑定按节点身份保持独立，重命名不影响 |
| A05 | 类型错误、端口删除、插件不兼容 | 草稿定位问题，发布明确阻止，无旧值冒充 |
| A06 | 可选 overlay 关闭、零值、空集合 | 分别显示缺失/合法零/合法空，不混成 NG |
| A07 | 直接绑定节点输出 | 无需额外全局变量、工作流出口或显示算子 |
| A08 | 同一输出在多页/多控件 | 一次采集，复用会话，无额外 StartJob/相机连接 |
| A09 | 循环体、同子流程 A/B 调用、嵌套调用 | 不跨调用串结果；歧义无法自动解析时阻止发布 |
| A10 | 失败、跳过、分支未命中、强制中止 | 本次结果状态明确，不沿用上一轮 OK/图像 |
| A11 | 图片乱序、延迟、丢失、被修改原缓冲 | 无错图/错框/错数值；旧请求不能覆盖新快照 |
| A12 | 图像 resize/crop/透视与叠加 | 源帧与坐标空间匹配，不靠猜测尺寸 |
| A13 | 快照与订阅竞态、重复消息 | 无缺口或明确 RESET_REQUIRED，重放不增加计数 |
| A14 | 换 Job、换项目、Runtime 重启 | generation/游标重置正确，无旧会话污染 |
| A15 | 慢消费者、图片出口拥塞 | 有界内存/磁盘；跳中间展示不丢正式事件 |
| A16 | 4 线程下长期订阅配额满 | 超限快速失败，停止/状态可响应，取消归还配额 |
| A17 | Job 完成/失败与导出竞态 | 资源接管或标不可用；无失效路径或无限收尾 |
| A18 | 1000 次切页、反复浮动开关 | 会话/线程/句柄不累增，隐藏页不持续解码 |
| A19 | 页面复制、排序、重命名、删除 | pageId 引用正确；副本绑定保持并提示，未决引用阻止发布 |
| A20 | 撤销删除及引用修复、重做、保存标记 | 模型一致，运行数据更新不污染撤销或脏标记 |
| A21 | 模拟、打开预览、关闭浮动页 | 不连接设备、不写计数、不隐式 StartJob/StopJob |
| A22 | 锁定某次 NG 详情、过期 | 不跳到下一次 OK；过期不替换最新图 |
| A23 | 当前集合分页、筛选、新快照 | 行不混批，类型正确，内存有界；不假装查询历史 |
| A24 | 草稿修改与运行版本 | 已发布现场版本不被热改，缺少采集源明确提示 |
| A25 | 非法包、越界路径、哈希错、超大资源 | 导入失败不触碰当前发布版，不执行包内脚本 |
| A26 | 连续打包和项目内输出路径 | 不递归打入旧包、DB、日志、缓存和敏感数据 |
| A27 | Windows 冻结 spawn、就绪、端口冲突 | 入口正确，无 Designer 递归启动，不误杀进程 |
| A28 | 关闭运行页面、独立 Runtime 仍在执行 | 只取消展示；内嵌模式另有明确生命周期说明 |
| A29 | DPI、窄窗口、中文、键盘、颜色区分 | 内容可读，关键状态不隐藏，导航可操作 |
| A30 | 原流程/算子编辑器/预览/图标/日志/计数 | 现有测试及针对性回归不退化，硬件测试单列 |

## 14. 性能预算与测试口径

不得写“Qt 天生零延迟”或“已达到生产指标”。P0 固定参考硬件、驱动/显示缩放、图像格式/尺寸、生产速率、页面/控件数、并发 Job/客户端数和输出持久化条件，再比较基线。

初始参考负载可用：一条模拟检测源、1920×1080 uint8 BGR、5 次结果/秒、5 页共约 50 个控件，分别运行 0/1/2 个显示客户端。循环、图像缺失和来源失效作为独立故障场景。

记录采图/算法耗时、结果封闭时间、图像编码/读取/解码、UI 整组提交时间、Start/Stop 响应、冷启动到服务/首页就绪，以及各进程 CPU/RSS、队列长度、磁盘和丢弃展示数量。禁止只测 gRPC RTT 代替可见延迟。

原型目标：参考负载下“结果封闭到同组 UI 提交”P95 不超过 200 ms；模拟检测节拍 P95 相对不开新增展示通道的基线回退不超过 5%。这只是待测门槛，不是当前实现结论；若基线太短、设备条件不同或目标无法达到，必须记录证据并显式调整范围/方案，不能只放宽指标后宣称原目标已满足。

同机跨进程使用同源单调计时验证，跨主机不得直接相减未校时墙钟；界面提交与实际显示器呈现的差异在验收中说明。进程隔离仍共享计算资源，展示预览缩放、缓冲复制和历史写盘成本都应计入。

测试分层：纯模型测试 → spawn/真实 gRPC → 真实 PySide2 事件循环 → Windows 冻结包 → 批准后的硬件联调。headless stub 通过不等于 GUI 通过；普通测试不接生产设备。截图只是证据之一，不替代消息序号、资源和断言记录。

现有完整检查入口为 `python scripts/ci_check.py`，内部顺序是 proto 一致性、Ruff、mypy、pytest。新增 proto 后按仓库流程重新生成并提交生成文件。环境准备沿用 [开发指南](../development-guide.md)，本次计划没有运行或宣称这套检查已通过。

## 15. 同分支实施纪律与首个开发任务

工作分支固定为 `agent/runtime-workflow-architecture-v1`。每阶段开始前读取最新 HEAD；本计划基线只是审查锚点，不能 reset 回锚点覆盖后续工作。不 force push，不自动 stash/丢弃用户改动；有本地修改先报告冲突范围。

建议提交粒度：契约/迁移 → 绑定目录 → 结果通道 → 共享客户端 → Qt 渲染器 → 页面编辑器 → 发布/入口 → 验收文档。各提交能独立审阅；核心执行语义、Qt 升级、设备协议改动不夹带进去。

首个实施任务是 P0，随后进入 P1；不是直接往 MainWindow 增加一个空页面标签。P0 的最小演示必须证明“指定节点真实输出 → 同次结果快照 → 两张页面共享显示”，并覆盖至少一个循环/重复子流程案例。

实施记录应逐项填写提交、测试命令/环境、证据路径及未通过项。未完成 P2 不标记“实时绑定可用”，未完成 P5 不标记“可交付运行包”，未完成真实硬件验收不标记“现场已验证”。

## 16. 参考与证据入口

仓库依据：[工程师导读](../engineer-guide.md)、[项目格式](../project-json-spec.md)、[事件流](../runtime-event-flow.md)、[算子 I/O](../operator-io-contracts.md)、[部署与发布](../deployment-guide.md)。本计划新增定义优先与这些文档在相应实施阶段同步，不把计划直接当已实现能力。

已核对的官方技术资料（2026-09-25）：

- [PySide2 QStackedWidget](https://doc.qt.io/archives/qtforpython-5/PySide2/QtWidgets/QStackedWidget.html)：页面宿主与对象所有权；removeWidget 不等于销毁资源。
- [PySide2 QUndoStack](https://doc.qt.io/archives/qtforpython-5/PySide2/QtWidgets/QUndoStack.html)：命令、宏、撤销重做与 clean 状态。
- [Qt Threads and QObjects](https://doc.qt.io/qt-6/threads-qobject.html)：线程归属和 GUI 线程原则；此文仅作原则参考，不引入 Qt6 专用 API，实现必须在 PySide2 上验证。
- [gRPC Performance Best Practices](https://grpc.io/docs/guides/performance/)：长期流和 Python 线程资源的限制，需要实测而不是按 UI 框架推断。

本次交付只是一份基于明确代码快照的实施计划；尚未提交功能代码、运行完整测试、测得性能指标或操作真实设备。
